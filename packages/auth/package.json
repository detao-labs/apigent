{
  "name": "@apigent/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Apigent Auth — shared Auth.js (NextAuth v5) config factory",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "format": "prettier --write src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@apigent/core": "workspace:*",
    "@apigent/server": "workspace:*",
    "next-auth": "5.0.0-beta.32"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  }
}
