{
  "name": "@apigent/open",
  "version": "0.1.0",
  "private": true,
  "description": "Apigent Open API Gateway — Hono (REST implemented; MCP planned, not yet implemented)",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "format": "prettier --write src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@apigent/core": "workspace:*",
    "@hono/node-server": "^1.0.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "hono": "^4.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  }
}
